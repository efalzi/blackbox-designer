var module = angular.module('view.designer', [
    'services.blackbox',
    'view.designer.palette',
    'view.designer.canvas']);

module.service('DesignerManager', ['Config'], function(config) {
    var designer = {

    };

    return designer;

});

